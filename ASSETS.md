# Assets

This prototype uses a generated visual direction expressed through CSS gradients, canvas shapes, typography, and Unicode heraldic/army symbols. No binary assets are required, so the game is portable as one HTML file and has no external asset URLs.
