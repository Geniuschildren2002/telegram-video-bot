# Memory

- Implemented as a single standalone `index.html` for easy deployment.
- Existing Python Telegram bot remains unchanged.
- Canvas uses fixed internal resolution with responsive CSS scaling.
- All gameplay is client-side and deterministic enough for manual browser testing.
